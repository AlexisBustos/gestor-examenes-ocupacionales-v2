DATABASE_URL="postgresql://user:password@host:port/dbname?schema=public" # conexión a AWS Postgres
PORT=3000            # puerto local; en Render será ignorado porque setean PORT
NODE_ENV=production  # optimizaciones de Express/Prisma
FRONTEND_URL="https://tu-dominio-vercel.app" # dominio del frontend en Vercel
JWT_SECRET="supersecretkey" # si el proyecto usa autenticación
CORS_ORIGIN=""       # opcional si lo usas como variable para CORS explícito
